# =========================================================================
#                      Autogenerated from Makefile.in         
# =========================================================================

prefix = @prefix@
exec_prefix = @exec_prefix@
INSTALL = @INSTALL@
top_srcdir = @top_srcdir@
top_wxsrcdir = @top_wxsrcdir@
top_builddir = @top_builddir@
top_wxbuilddir = @top_wxbuilddir@
CXX = @CXX@
MONGOOSE_FLAGS = -DMG_ENABLE_THREADS -DMG_ENABLE_IPV6 -DMG_ENABLE_SSL -DMG_ENABLE_GETADDRINFO
CXXFLAGS = -g -O2  -fPIC $(MONGOOSE_FLAGS) -I../../../../common -I../../../../../common
CPPFLAGS = -I include  @CPPFLAGS@ -D MG_ENABLE_THREADS -fPIC -I../../../../common -I../../../../../common
CFLAGS = -fPIC -I../../../../common -I../../../../../common
LDFLAGS = @LDFLAGS@
EXTRALIBS = @EXTRALIBS@
DLFLAGS = -g -shared -fPIC
TRIP = strip
INSTALL_PROGRAM = ${INSTALL}
INSTALL_DATA = ${INSTALL} -m 644
INSTALL_DIR = /usr/bin/install -c -d
srcdir = .
top_srcdir = .
top_builddir =
bindir = ${exec_prefix}/bin
libdir = ${exec_prefix}/lib
datadir = ${prefix}/share
includedir = ${prefix}/include
DLLPREFIX = lib


### Variables: ###

WX_RELEASE = 3.0
LIBDIRNAME = $(top_wxbuilddir)lib

MQTT_OBJECTS = ../common/mqttobj.o\
	../../../../common/vscpremotetcpif.o\
	../../../../common/vscphelper.o\
	../../../../common/guid.o\
	../../../../../common/crc.o\
	../../../../../common/crc8.o\
	../../../../../common/mongoose.o\

	
PROJOBJS =  ../common/mqttobj.o\
	../../../../common/vscpremotetcpif.o\
	../../../../common/vscphelper.o\
	../../../../common/guid.o\
	../../../../../common/crc.o\
	../../../../../common/crc8.o\
	../../../../../common/mongoose.o\

ALWAYS_BUILD = 	../../../../common/vscpremotetcpif.o\
	../../../../common/vscphelper.o\
	../../../../common/guid.o\
	../../../../../common/crc.o\
	../../../../../common/crc8.o\
	../../../../../common/mongoose.o\

### Targets: ###

all: vscpl2drv_mqtt.so test

install: all
	$(INSTALL_PROGRAM) vscpl2drv_mqtt.so $(libdir)

uninstall: 

install-strip: install

clean: 
	rm -rf ./.deps ./.pch
	rm -f ./*.o
	rm -f ../common/*.o
	rm -f ../../../common/*.o
	rm -f ../../../../common/*.o
	rm -f ../../../../../common/*.o
	rm -f vscpl2drv_mqtt.so
	rm -f test

distclean: clean
	rm -f config.cache config.log config.status bk-deps bk-make-pch shared-ld-sh Makefile


test:  test.o $(PROJOBJS)
	$(CXX) -o $@ test.o $(PROJOBJS) $(LDFLAGS) -L$(LIBDIRNAME) $(WXLIBS) $(EXTRALIBS)

# Build the Linux DL
%.so: %.o $(MQTT_OBJECTS)
	$(CXX) -o $@ vscpl2drv_mqtt.o $(MQTT_OBJECTS) $(DLFLAGS) -L$(LIBDIRNAME) $(WXLIBS) $(EXTRALIBS) 

$(ALWAYS_BUILD):  .FORCE

.FORCE: 

# Include dependency info, if present:
-include .deps/*.d

.PHONY: all install uninstall clean distclean data .FORCE



