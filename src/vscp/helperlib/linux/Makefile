# =========================================================================
#                      Autogenerated from Makefile.in         
# =========================================================================

prefix = /usr/local
exec_prefix = ${prefix}
INSTALL = /usr/bin/install -c
top_srcdir = ../../../..
top_wxsrcdir = @top_wxsrcdir@
top_builddir = ../../../..
top_wxbuilddir = @top_wxbuilddir@
CXX = g++
CXXFLAGS = -g -O2  -fPIC -D__LINUX__ 
CPPFLAGS =  -I/usr/local/lib/wx/include/gtk2-unicode-3.0 -I/usr/local/include/wx-3.0 -D_FILE_OFFSET_BITS=64 -DWXUSINGDLL -D__WXGTK__ -D NS_ENABLE_SSL -fPIC
CFLAGS = -fPIC -D__LINUX__ 
LDFLAGS = 
EXTRALIBS =  -lmosquittopp -lssl -lpcap -lz -lrt -lm -lcrypto -lpthread -ldl -lcurses  -L/usr/local/lib -pthread   -lwx_gtk2u_xrc-3.0 -lwx_gtk2u_html-3.0 -lwx_gtk2u_qa-3.0 -lwx_gtk2u_adv-3.0 -lwx_gtk2u_core-3.0 -lwx_baseu_xml-3.0 -lwx_baseu_net-3.0 -lwx_baseu-3.0 
DLFLAGS = -g -shared -fPIC
TRIP = strip
INSTALL_PROGRAM = ${INSTALL}
INSTALL_DATA = ${INSTALL} -m 644
INSTALL_DIR = /usr/bin/install -c -d
srcdir = .
top_srcdir = .
top_builddir =
bindir = ${exec_prefix}/bin
libdir = ${exec_prefix}/lib
datadir = ${prefix}/share
includedir = ${prefix}/include
DLLPREFIX = lib


### Variables: ###

WX_RELEASE = 2.8
LIBDIRNAME = $(top_wxbuilddir)lib

VSCPHELPER_OBJECTS =  libvscphelper.o ../vscphelperlib.o ../../common/vscpremotetcpif.o ../../common/vscphelper.o ../../common/guid.o ../../../common/crc.o ../../../common/crc8.o ../../../common/net_skeleton.o 
PROJOBJS        =  ../vscphelperlib.h ../../common/vscpremotetcpif.o ../../common/vscphelper.o  ../../../common/crc.o ../../common/guid.o ../../../common/crc8.o ../../../common/net_skeleton.o



### Targets: ###

all: libvscphelper.so test lib

install: all
	$(INSTALL_PROGRAM) libvscphelper.so $(libdir)
	ar rcs libvscphelper.a libvscphelper.o

uninstall: 

install-strip: install

clean: 
	rm -rf ./.deps ./.pch
	rm -f ./*.o
	rm -f ./*.a
	rm -f ../common/*.o
	rm -f ../../common/*.o
	rm -f ../../../common/*.o
	rm -f ../../../../common/*.o
	rm -f libvscphelper.so
	rm -f test

distclean: clean
	rm -f config.cache config.log config.status bk-deps bk-make-pch shared-ld-sh Makefile

lib:   libvscphelper.so
	ar rcs libvscphelper.a libvscphelper.o

test:  test.o $(PROJOBJS)
	$(CXX) -o $@ test.o $(PROJOBJS) $(LDFLAGS) -L$(LIBDIRNAME) $(WXLIBS) $(EXTRALIBS)

# Build the Linux DL
%.so: %.o $(VSCPHELPER_OBJECTS)
	$(CXX) -o $@ $(VSCPHELPER_OBJECTS) $(DLFLAGS) -L$(LIBDIRNAME) $(WXLIBS) $(EXTRALIBS) 

# Include dependency info, if present:
-include .deps/*.d

.PHONY: all install uninstall clean distclean data



