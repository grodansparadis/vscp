# =========================================================================
#                      Autogenerated from Makefile.in         
# =========================================================================

prefix = /usr/local
exec_prefix = ${prefix}
INSTALL = /usr/bin/install -c
top_srcdir = ../../..
top_wxsrcdir = @top_wxsrcdir@
top_builddir = ../../..
top_wxbuilddir = /usr/local
CXX = g++
CXXFLAGS = -g -O2 -I/usr/local/lib/wx/include/gtk2-ansi-release-2.8 -I/usr/local/include/wx-2.8 -D_FILE_OFFSET_BITS=64 -D_LARGE_FILES -D__WXGTK__ -pthread
CPPFLAGS = 
LDFLAGS =  -L/usr/local/lib -pthread   -lwx_gtk2_aui-2.8 -lwx_gtk2_xrc-2.8 -lwx_gtk2_qa-2.8 -lwx_gtk2_html-2.8 -lwx_gtk2_adv-2.8 -lwx_gtk2_core-2.8 -lwx_base_xml-2.8 -lwx_base_net-2.8 -lwx_base-2.8 
EXTRALIBS =  -pthread -W1,--version-script -lz -ldl -lm -lcrypto
STRIP = strip
INSTALL_PROGRAM = ${INSTALL}
INSTALL_DATA = ${INSTALL} -m 644
INSTALL_DIR = /usr/bin/install -c -d
srcdir = .
top_srcdir = .
top_builddir =
bindir = ${exec_prefix}/bin
libdir = ${exec_prefix}/lib
datadir = ${prefix}/share
includedir = ${prefix}/include
DLLPREFIX = lib


### Variables: ###

DESTDIR = 
WX_RELEASE = 2.8
LIBDIRNAME = $(top_wxbuilddir)lib

CANALLOGWND_OBJECTS =  canallogwnd.o optiondialog. 


### Targets: ###

all: canallogwnd

install: all
	$(INSTALL) canallogwnd $(bindir)

uninstall: 

install-strip: install

clean: 
	rm -rf ./.deps ./.pch
	rm -f ./*.o
	rm -f ../common/*.o
	rm -f ../../common/*.o
	rm -f canallogwnd

distclean: clean
	rm -f config.cache config.log config.status bk-deps bk-make-pch shared-ld-sh Makefile

canallogwnd: $(CANALLOGWND_OBJECTS)
	$(CXX) -o $@ $(CANALLOGWND_OBJECTS) $(LDFLAGS) -L$(LIBDIRNAME) $(WXLIBS) $(EXTRALIBS)



# Include dependency info, if present:
-include .deps/*.d

.PHONY: all install uninstall clean distclean data

