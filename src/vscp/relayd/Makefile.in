# =========================================================================
#                      Autogenerated from Makefile.in
# =========================================================================

VSCP_PROJ_BASE_DIR=@VSCPDIR@

prefix = @prefix@
exec_prefix = @exec_prefix@
INSTALL = @INSTALL@
top_srcdir = @top_srcdir@
top_builddir = @top_builddir@

CC = @CC@
CXX = @CXX@

# get version from the version file
MAJOR_VERSION = `sed '35!d' ../../../../common/version.h  | cut -b 33-`
MINOR_VERSION = `sed '36!d' ../../../../common/version.h  | cut -b 33-`
RELEASE_VERSION = `sed '37!d' ../../../../common/version.h  | cut -b 33-`
BUILD_VERSION = `sed '38!d' ../../../../common/version.h  | cut -b 33-

CFLAGS = @CFLAGS@ -fPIC  -DCBC -I. -I../common -I../../common
LDFLAGS = @LDFLAGS@
EXTRALIBS = @EXTRALIBS@ -lcrypt

STRIP = strip
INSTALL_PROGRAM = ${INSTALL}
INSTALL_DATA = ${INSTALL} -m 644
INSTALL_DIR = /usr/bin/install -c -d
srcdir = .
top_srcdir = .
top_builddir =
bindir = ${exec_prefix}/bin
libdir = ${exec_prefix}/lib
datadir = ${prefix}/share
includedir = ${prefix}/include
DLLPREFIX = lib

### Variables: ###

RELAYD_OBJECTS = relayd.o\
	sockettcp.o\
	vscpmd5.o\
	canalif.o\

### Targets: ###

all: relayd

relayd:  $(RELAYD_OBJECTS)
	$(CC) -o vscp-relayd $(RELAYD_OBJECTS) $(LDFLAGS)  $(EXTRALIBS)

relayd.o: relayd.c relayd.h
	$(CC) $(CFLAGS)  -c relayd.c -o $@

sockettcp.o: ../../common/sockettcp.c ../../common/sockettcp.h
	$(CC) $(CFLAGS) -c ../../common/sockettcp.c -o $@

canalif.o: canalif.c canalif.h
	$(CC) $(CFLAGS) -c canalif.c -o $@

dllist.o: ../../common/dllist.c ../../common/dllist.h
	$(CC) $(CFLAGS) -c ../../common/dllist.c -o $@

crc8.o: ../../common/crc8.c ../../common/crc8.h
	$(CC) $(CFLAGS) -c ../../common/crc8.c -o $@

vscpmd5.o: ../../common/vscpmd5.c ../../common/vscpmd5.h
	$(CC) $(CFLAGS) -c ../../common/vscpmd5.c -o $@	
	
install: all
	$(INSTALL_PROGRAM) -d $(VSCP_PROJ_BASE_DIR)

uninstall:

install-strip: install

deb:    all
	echo "\n" | ./builddist.sh
	@mkdir -p ../../../../../../dist
	@mv *.deb ../../../../../../dist

clean:
	rm -rf vscp-relayd
	rm -rf ./.deps ./.pch
	rm -f ./*.o
	rm -f ../common/*.o
	rm -f ../../../../common/*.o
	rm -f ../../../../../common/*.o
	rm -rf *~
	rm -f *.gz
	rm -f *.deb

distclean: clean
	rm -f config.cache config.log config.status bk-deps bk-make-pch shared-ld-sh

$(ALWAYS_BUILD):  .FORCE

.FORCE:

# Include dependency info, if present:
-include .deps/*.d

.PHONY: all install uninstall clean distclean data .FORCE
