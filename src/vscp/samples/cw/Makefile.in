# =========================================================================
#                      Autogenerated from Makefile.in         
# =========================================================================

prefix = @prefix@
exec_prefix = @exec_prefix@
INSTALL = @INSTALL@
top_srcdir = @top_srcdir@
top_wxsrcdir = @top_wxsrcdir@
top_builddir = @top_builddir@
top_wxbuilddir = @top_wxbuilddir@
CXX = @CXX@
CXXFLAGS = @CXXFLAGS@
CPPFLAGS = @CPPFLAGS@
LDFLAGS = @LDFLAGS@
EXTRALIBS = @EXTRALIBS@
STRIP = strip
INSTALL_PROGRAM = ${INSTALL}
INSTALL_DATA = ${INSTALL} -m 644
INSTALL_DIR = /usr/bin/install -c -d
srcdir = .
top_srcdir = .
top_builddir =
bindir = ${exec_prefix}/bin
libdir = ${exec_prefix}/lib
datadir = ${prefix}/share
includedir = ${prefix}/include
DLLPREFIX = lib


### Variables: ###

DESTDIR = 
WX_RELEASE = 2.8
LIBDIRNAME = $(top_wxbuilddir)lib

CANALWORKS_OBJECTS =  cw.o  \
	../../common/canalsuperwrapper.o \
        ../../common/vscptcpif.o \
	../../common/vscphelper.o \
	../../../common/crc.o \
	../../../common/configfile.o \
	../../../common/dllist.o \
	../../common/dllwrapper.o \
        ../../common/guid.o \
	editifdlg.o \
	msglistwnd.o \
	msglistwnd_level2.o \
	selectifdlg.o \
	vscpconfigwnd.o


### Targets: ###

all: cw

install: all
	$(INSTALL) cw $(bindir)

uninstall: 

install-strip: install

clean: 
	rm -rf ./.deps ./.pch
	rm -f ./*.o
	rm -f ../common/*.o
	rm -f ../../common/*.o
	rm -f cw

distclean: clean
	rm -f config.cache config.log config.status bk-deps bk-make-pch shared-ld-sh Makefile

cw: $(CANALWORKS_OBJECTS)
	$(CXX) -o $@ $(CANALWORKS_OBJECTS) $(LDFLAGS) -L$(LIBDIRNAME) $(WXLIBS) $(EXTRALIBS)
	


# Include dependency info, if present:
-include .deps/*.d

.PHONY: all install uninstall clean distclean data

