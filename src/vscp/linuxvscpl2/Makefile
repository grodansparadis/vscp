# =========================================================================
#                      Autogenerated from Makefile.in         
# =========================================================================

prefix = /usr/local
exec_prefix = ${prefix}
INSTALL = /usr/bin/install -c
top_srcdir = ../../..
top_wxsrcdir = @top_wxsrcdir@
top_builddir = ../../..
top_wxbuilddir = /usr/local
CXX = g++
CXXFLAGS = -g -O2 -I/usr/local/lib/wx/include/gtk2-unicode-debug-2.8 -I/usr/local/include/wx-2.8 -D_FILE_OFFSET_BITS=64 -D_LARGE_FILES -D__WXDEBUG__ -D__WXGTK__ -pthread -fPIC -fPIC
CPPFLAGS =  -fPIC -fPIC
CFLAGS = -fPIC
LDFLAGS =  -L/usr/local/lib -pthread   -lwx_gtk2ud_aui-2.8 -lwx_gtk2ud_xrc-2.8 -lwx_gtk2ud_qa-2.8 -lwx_gtk2ud_html-2.8 -lwx_gtk2ud_adv-2.8 -lwx_gtk2ud_core-2.8 -lwx_baseud_xml-2.8 -lwx_baseud_net-2.8 -lwx_baseud-2.8 
EXTRALIBS =  -pthread -W1,--version-script -lz -ldl -lm -lcrypto
DLFLAGS = -g -shared -fPIC
TRIP = strip
INSTALL_PROGRAM = ${INSTALL}
INSTALL_DATA = ${INSTALL} -m 644
INSTALL_DIR = /usr/bin/install -c -d
srcdir = .
top_srcdir = .
top_builddir =
bindir = ${exec_prefix}/bin
libdir = ${exec_prefix}/lib
datadir = ${prefix}/share
includedir = ${prefix}/include
DLLPREFIX = lib


### Variables: ###

WX_RELEASE = 2.6
LIBDIRNAME = $(top_wxbuilddir)lib

VSCPL1_OBJECTS =  vscpl1.o ../common/vscpremotetcpif.o ../common/vscp.o ../../common/crc.o
PROJOBJS        =  ../common/vscpremotetcpif.o ../common/vscp.o ../../common/crc.o



### Targets: ###

all: vscpl1.so test

install: all
	$(INSTALL_PROGRAM) vscpl1.so $(libdir)

uninstall: 

install-strip: install

clean: 
	rm -rf ./.deps ./.pch
	rm -f ./*.o
	rm -f ../common/*.o
	rm -f ../../../common/*.o
	rm -f ../../../../common/*.o
	rm -f vscpl1.so
	rm -f test

distclean: clean
	rm -f config.cache config.log config.status bk-deps bk-make-pch shared-ld-sh Makefile

#canallogger: $(VSCPL1_OBJECTS)
#	$(CXX) -o $@ $< $(VSCPL1_OBJECTS) $(LDFLAGS) -L$(LIBDIRNAME) $(WXLIBS) $(EXTRALIBS)
	

test:  test.o $(PROJOBJS)
	$(CXX) -o $@ test.o $(PROJOBJS) $(LDFLAGS) -L$(LIBDIRNAME) $(WXLIBS) $(EXTRALIBS)

# Build the Linux DL
%.so: %.o $(VSCPL1_OBJECTS)
	$(CXX) -o $@ $(VSCPL1_OBJECTS) $(DLFLAGS) -L$(LIBDIRNAME) $(WXLIBS) $(EXTRALIBS) 

# Include dependency info, if present:
-include .deps/*.d

.PHONY: all install uninstall clean distclean data



