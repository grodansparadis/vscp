# =========================================================================
#                      Autogenerated from Makefile.in         
# =========================================================================

prefix = @prefix@
exec_prefix = @exec_prefix@
INSTALL = @INSTALL@
top_srcdir = @top_srcdir@
top_wxsrcdir = @top_wxsrcdir@
top_builddir = @top_builddir@
top_wxbuilddir = @top_wxbuilddir@
CXX = @CXX@
CXXFLAGS = @CXXFLAGS@
CPPFLAGS = @CPPFLAGS@ -I../common -I../../common
CFLAGS= -std=c99 -D _POSIX_SOURCE -D _SVID_SOURCE @CFLAGS@
LDFLAGS = @LDFLAGS@
EXTRALIBS = @EXTRALIBS@
STRIP = strip
INSTALL_PROGRAM = ${INSTALL}
INSTALL_DATA = ${INSTALL} -m 644
INSTALL_DIR = /usr/bin/install -c -d
srcdir = .
top_srcdir = .
top_builddir =
bindir = ${exec_prefix}/bin
libdir = ${exec_prefix}/lib
datadir = ${prefix}/share
includedir = ${prefix}/include
DLLPREFIX = lib

### Variables: ###

DESTDIR = 
WX_RELEASE = 3.1
LIBDIRNAME = $(top_wxbuilddir)lib

UVSCPD_OBJECTS =  uvscpd.o\
	websocket.o\
	base64_enc.o\
	sha1.c \
	../../common/dllist.o \
	../../common/crc.o \
	../../common/crc8.o \
	../../common/aes.o\

ALWAYS_BUILD = ../../common/dllist.o \
	../../common/crc.o \
	../../common/crc8.o \
	../../common/aes.o \
	../../common/randpassword.o\

### Targets: ###

all: uvscpd

install: all
	$(INSTALL_PROGRAM) uvscpd $(bindir)

uninstall: 

install-strip: install

clean: 
	rm -rf ./.deps ./.pch
	rm -f ./*.o
	rm -f ../common/*.o
	rm -f ../../common/*.o
	rm -f uvscpd

distclean: clean
	rm -f config.cache config.log config.status bk-deps bk-make-pch shared-ld-sh Makefile

uvscpd: $(UVSCPD_OBJECTS)
	$(CXX) -o $@ $(UVSCPD_OBJECTS) $(LDFLAGS) -L$(LIBDIRNAME) $(WXLIBS) $(EXTRALIBS)
	
$(ALWAYS_BUILD):  .FORCE

.FORCE: 

# Include dependency info, if present:
-include .deps/*.d

.PHONY: all install uninstall clean distclean data .FORCE

