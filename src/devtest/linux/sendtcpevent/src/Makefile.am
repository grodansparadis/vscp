bin_PROGRAMS = sendtcpevent
sendtcpevent_SOURCES = sendtcpevent.c

# set the include path found by configure
INCLUDES= $(all_includes)

# the library search path.
sendtcpevent_LDFLAGS = $(all_libraries) 
